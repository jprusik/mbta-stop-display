export enum VehicleTypeKeyName {
  BUS = 'bus',
  FERRY = 'ferry',
  TRAIN = 'train'
}

// key name is `vehicle_type` on Stop model, `type` on Route model
export enum VehicleType {
  LIGHT_RAIL = 0,
  HEAVY_RAIL = 1,
  COMMUTER_RAIL = 2,
  BUS = 3,
  FERRY = 4
}
